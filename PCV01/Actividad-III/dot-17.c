#include <stdio.h>

int main()
{
   int x = 1;

   printf("x++ produce: %d\n", x++);

   printf("Ahora x contiene: %d\n", x);

   printf("x = X++ produces: %d\n", x++);

   printf("Now x contains: %d\n", x);
   return 0;
}
